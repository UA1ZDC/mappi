В xml хранятся предельные значение для проверки метео-данных.

Правила:
   1 Обязательно должна быть указана категория для которой используются данные. Тег category, значения перечисляются через запятую в атрибуте num.
   2 Условия проверки скомпанованы в группы group. Внутри группы может быть несколько блоков: codeform - если внутри проверяется кодовое значение; phys - если проверяется величина.
   3 Проверка выполняется для каждой группы для одного из блоков, который подойдёт по условиям match.
   4 Тег match используется для проверки условия для использования набора правил контроля.
   5 Тег val - правило контроля. В нём обязательно должно присутствовать название параметра - атрибут name (соответствует descriptors.xml).

Подробности и особенности в самих xml.