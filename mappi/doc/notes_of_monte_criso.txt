БАЗОВЫЕ ПОНЯТИЯ
---------------
Сеанс - расчитаный временной период пролета спутника над пунктом приема, согласно заданным условиям приёма.
Фазы прохождения сеанса:
- ожидание (может быть пропущен при пересечении сеансов);
- подготовка - настройка всей аппаратуры;
- сопровождение;
- завершен (состояние используется расписанием).

Активный сеанс - сеанс который уже находится в обработке, т.е. он выбран диспетчером dispatcher.service для приёма.
Сервисный режим - переход сервиса в этот режим означает прекращение работы в автоматическом режиме.


МИКРОСЕРВИСНАЯ АРХИТЕКТУРА
--------------------------

schedule.service
----------------
Основная задача - работа с расписанием:
- формирование согласно конфигурации;
- формирование по требованию;
- выдача расписания;
- выдача следующего и ближайшего сеанса (вместе с сеансом передаются и tle-параметры спутника);
- отслеживание изменение tle-файла (при изменении обновляется расписание);
- выдача tle-параметров заданного спутника из tle-файла;
- выдача информации о всех спутниках;
- ручное решение конфликтов.

Сервис работает по принципу "ленивой инициализации", т.е. пока кому-нибудь не понадобится расписание они не будет сформировано.
Сформированное текущее расписание сохраняется в файл.

В случае изменения расписания другие сервисы НЕ УВЕДОМЛЯЮТСЯ об этом.
Расписание может поменяться по следующим причинам:
- ручное решение конфликтов, влияет на то будет приниматься сеанс или нет;
- изменение tle-файла.

Основные клиента:
- GUI (запрос расписания, ручное решение конфликтов);
- dispatcher.service - ответ на запрос о следующем и ближайшем сеансе. Расписание не передается целиком, требуется только информация о ближайшем сеансе.

ВНИМАНИЕ
- При решении конфликтов целесообразно останавливать прием по расписанию,
  т.к. может получиться ситуация, когда активный сеанс исключен пользователем из расписания и
  по логике работы dispatcher.service этот сеанс все равно будет принят.
- На сегодняшний день при формировании нового расписания по требованию, если до этого, конфликты решались вручную, то они будут потеряны.
- Конфликты решенные пользователем не сохраняются в следующих случаях (со слов Юры):
  * при обнвленни tle-файла, файл может сильно устареть, поэтому расписание сильно может разъехаться;
  * при регенерации расписания (раз в сутки добавление следующего дня), на стыке дней могут произойти новые конфликты.
- Расмотреть возможность передачи информации о tle-файле (когда обновлен, когда сформирован и т.д.)

dispatcher.service
------------------
Поддерживает два режима работы:
- автоматический - работа по расписанию, выдерживаются все временные интервалы, работа в реальном времени.
- демо - демострационный режим, работа так же идет по расписанию, но сеансы принимаются друг за другом, без учета фазы ожидания.

Сервис является входной точкой для включения и выключения сервсиного режима
Поддерживает сервисный режим
Является оркестратором для следующих сервисов:
- receiver.service
- antenna.service

Включение\отключение
Пользователь


receiver.service
------------------
Основная задача - работа с приемником:
- 

Управляется

ВНИМАНИЕ
- Необходимо определиться с тестовым режимом

antenna.service
------------------